kind: pipeline
type: ssh
name: drone-test

clone:
  disable: true

server:
  host: 172.31.15.235
  user: abi-test
  password: password

steps:
- name: greeting
  commands:
  - echo halo from release
  when:
    branch:
    - release
    event:
      - push
- name: greeting 2
  commands:
  - echo halo from master
  when:
    branch:
    - master
    event:
      - push
